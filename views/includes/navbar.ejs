<nav class="navbar navbar-expand-lg navbar-dark bg-dark shadow">
  <div class="container-fluid">

    <!-- Logo + Name -->
    <a class="navbar-brand d-flex align-items-center" href="/listings">
      <img src="/images/icon.png" width="30" height="30" class="me-2">
      WanderLust
    </a>

    <!-- Mobile Toggle Button -->
    <button
      class="navbar-toggler"
      type="button"
      data-bs-toggle="collapse"
      data-bs-target="#navbarNav">
      <span class="navbar-toggler-icon"></span>
    </button>

    <!-- Navbar Options -->
    <div class="collapse navbar-collapse" id="navbarNav">

      <!-- Left side -->
      <ul class="navbar-nav me-auto">

        <li class="nav-item">
          <a class="nav-link" href="/listings">Home</a>
        </li>

        <% if (currentUser) { %>
          <li class="nav-item">
            <a class="nav-link" href="/listings/new">Add Listing</a>
          </li>
        <% } %>

      </ul>

      <div class="d-flex align-items-center">
        <% if (currentUser) { %>
          <span class="text-white me-3">Signed in as <%= currentUser.username %></span>
          <form action="/logout" method="POST" class="m-0">
            <button class="btn btn-outline-light btn-sm" type="submit">Logout</button>
          </form>
        <% } else { %>
          <a class="btn btn-outline-light btn-sm me-2" href="/login">Login</a>
          <a class="btn btn-outline-light btn-sm" href="/register">Sign Up</a>
        <% } %>
      </div>

    </div>

  </div>
</nav>